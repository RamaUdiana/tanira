<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - TANIRA</title>
    <link rel="icon" href="/logo/logo.png" type="image/png">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">

    <!-- Tailwind CSS -->
    <link rel="stylesheet" href="/css/output.css">
    <link rel="stylesheet" href="/css/custom.css">
</head>
<body class="font-sans text-tanira-dark bg-tanira-cream antialiased">

    <div class="min-h-screen flex">
        <!-- Left Side -->
        <div class="hidden lg:flex w-1/2 bg-tanira-greenDark relative items-center justify-center overflow-hidden">
            <img src="https://images.unsplash.com/photo-1500937386664-56d1dfef3854?q=80&w=1920&auto=format&fit=crop" class="absolute inset-0 w-full h-full object-cover opacity-40 mix-blend-overlay">
            <div class="relative z-10 p-12 text-white max-w-lg">
                <h2 class="text-4xl font-bold leading-tight mb-6">"Data hasil panen Anda aman bersama kami."</h2>
            </div>
        </div>

        <!-- Right Side -->
        <div class="w-full lg:w-1/2 flex items-center justify-center p-8 bg-tanira-cream relative min-h-screen">
            <a href="/auth/login" class="absolute top-8 left-8 flex items-center gap-2 text-sm text-tanira-gray hover:text-tanira-green transition-colors z-20 font-medium">
                <i class="fa-solid fa-arrow-left"></i> Kembali ke Login
            </a>

            <div class="w-full max-w-md pt-16 lg:pt-0">
                <div class="text-center lg:text-left mb-8">
                    <div class="w-16 h-16 bg-green-50 rounded-2xl flex items-center justify-center text-tanira-green mb-6 mx-auto lg:mx-0 border border-green-100">
                        <i class="fa-solid fa-key text-3xl"></i>
                    </div>
                    <h2 class="text-3xl font-bold text-tanira-dark mb-2">Lupa Password? 🔒</h2>
                    <p class="text-tanira-gray">Masukkan email yang terdaftar, kami akan mengirimkan instruksi reset password.</p>
                </div>

                <!-- ALERTS -->
                <% if (error) { %>
                <div class="bg-red-50 border-l-4 border-red-500 text-red-700 p-4 mb-6 rounded-r">
                    <p class="font-bold">Gagal</p>
                    <p><%= error %></p>
                </div>
                <% } %>

                <% if (success) { %>
                <div class="bg-green-50 border-l-4 border-green-500 text-green-700 p-4 mb-6 rounded-r">
                    <p class="font-bold">Terkirim!</p>
                    <p><%= success %></p>
                </div>
                <% } %>

                <!-- FORM -->
                <form action="/auth/forgot-password" method="POST" class="space-y-6">
                    <div>
                        <label class="block text-sm font-bold text-tanira-dark mb-2">Email</label>
                        <div class="relative">
                            <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none text-tanira-gray/50">
                                <i class="fa-regular fa-envelope"></i>
                            </div>
                            <input type="email" name="email" required placeholder="nama@email.com" class="w-full pl-11 pr-4 py-3 bg-white border border-gray-200 rounded-xl focus:outline-none focus:border-tanira-green focus:ring-2 focus:ring-tanira-green/20 transition-all text-sm font-medium shadow-sm">
                        </div>
                    </div>

                    <button type="submit" class="w-full bg-tanira-green hover:bg-tanira-greenDark text-white font-bold py-3.5 rounded-xl shadow-lg shadow-tanira-green/30 transition-all transform hover:-translate-y-0.5 active:scale-95">
                        Kirim Link Reset
                    </button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>