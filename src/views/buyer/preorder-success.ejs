<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" href="/logo/logo.png" type="image/png">
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="https://cdn.tailwindcss.com"></script>
    
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        tanira: {
                            green: '#2F7D32',
                            brown: '#6D4C41',
                            cream: '#F9F7F2',
                            dark: '#1F2937',
                            gray: '#6B7280',
                        }
                    },
                    fontFamily: { sans: ['Plus Jakarta Sans', 'sans-serif'] },
                    boxShadow: { 'soft': '0 4px 20px -2px rgba(0, 0, 0, 0.05)' }
                }
            }
        }
    </script>
</head>
<body class="bg-tanira-cream text-tanira-dark font-sans min-h-screen flex items-center justify-center p-6">

    <main class="w-full max-w-sm text-center">
        
        <div class="mb-8 flex justify-center">
            <div class="w-24 h-24 bg-green-100 rounded-full flex items-center justify-center text-tanira-green text-5xl animate-bounce shadow-lg shadow-green-100">
                <i class="fa-solid fa-check"></i>
            </div>
        </div>

        <h1 class="text-2xl font-bold text-tanira-dark mb-2">Pre-Order Berhasil!</h1>
        <p class="text-tanira-gray mb-8">
            Pesanan Anda untuk <strong><%= order.quantity %> Kg <%= order.listing.crop.name %></strong> telah dicatat. Petani akan diberitahu segera.
        </p>

        <div class="bg-white p-4 rounded-xl shadow-sm mb-8 text-left border border-gray-50">
            <div class="flex justify-between mb-2">
                <span class="text-xs text-gray-400">Total Estimasi</span>
                <span class="text-sm font-bold text-tanira-green"><%= helpers.formatRupiah(order.totalPrice) %></span>
            </div>
            <div class="flex justify-between">
                <span class="text-xs text-gray-400">Status</span>
                <span class="text-xs font-bold bg-gray-100 text-gray-500 px-2 py-0.5 rounded uppercase">Menunggu Panen</span>
            </div>
        </div>

        <div class="space-y-3">
            <a href="/market/orders" class="block w-full bg-tanira-green text-white font-bold py-3.5 rounded-xl hover:bg-tanira-greenDark transition shadow-lg shadow-tanira-green/30">
                Lihat Status Pesanan
            </a>
            <a href="/market" class="block w-full py-3.5 text-tanira-gray font-bold text-sm hover:text-tanira-dark transition">
                Kembali ke Marketplace
            </a>
        </div>

    </main>

</body>
</html>